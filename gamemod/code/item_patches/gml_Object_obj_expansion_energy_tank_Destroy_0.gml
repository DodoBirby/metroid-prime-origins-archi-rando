/*
if (!dz("Classic Mode"))
{
    if (ds_zero("Energy Tanks Max") == 4)
    {
        description += txt("\nYou can now use up to 3 active Aeon Powers.");
    }
    if (ds_zero("Energy Tanks Max") == 8)
    {
        description += txt("\nYou can now use up to 4 active Aeon Powers.");
    }
    if (ds_zero("Energy Tanks Max") == 14)
    {
        description += txt("\nYou can now use up to 5 active Aeon Powers.");
    }
}
*/
event_inherited();
